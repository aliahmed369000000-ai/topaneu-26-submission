# Model weights for TopAneu-26 Task 1

- `topaneu_task1_fold{0-4}.pt` — 5-fold ensemble (full model objects)
- `thresholds.json` — OOF calibration (global_threshold=0.15 preferred)

On Grand Challenge, upload a tarball of this folder as the **Algorithm Model**.
Weights are extracted to `/opt/ml/model`.
